development:
  sessions:
    default:
      database: mongoid
      hosts:
        - localhost:27017
      options:
        max_retries: 5
        retry_interval: 3
  options:
    raise_not_found_error: false
